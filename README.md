```
install nodejs
install npm
```

do `nvm use 16` to use version 16, don't ask why, it just works with 16 rn

```
$ npm install express socket.io uuid cookie-parser
```

inside /uno, run
```
$ node app.js
```
and then go to `localhost:4000` in browser
